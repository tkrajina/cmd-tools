#!/bin/sh

# ----------------------------------------------------------------------------------------------------
# Set terminal window title
# ----------------------------------------------------------------------------------------------------

title=$1

if [ -z "$title" ]
then
    title=$(pwd | sed -E "s/.*\/(.*)/\1/")
    echo "Current dir: $title"
fi

echo "Window title: $title"
echo "\033]0;$title\a"
